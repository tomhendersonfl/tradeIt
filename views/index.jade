extends layout

block content
  
  body.splash
      
  
    div.container  
      form(action='/auth/facebook' method='GET')
        if !user
          button.btn.waves-effect.waves-light(type="submit" name='submit' value='log in with facebook') Facebook Login
        if user
          p welcome #{user.displayName}
    div.centerLogo
      img(src="../img/tradeit_1024.png")


  script(src='https://code.jquery.com/jquery-2.1.1.min.js')
  script(src='http://cdn.jsdelivr.net/jquery.cookie/1.4.1/jquery.cookie.min.js')
  script(src='js/age-verification.js')
